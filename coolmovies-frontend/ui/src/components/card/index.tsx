import React from 'react';
import { Box } from './styles';

interface CardProps {
  title: string,
  release: string
}

const Card: React.FC<CardProps> = ({ title, release }) => {
  return (
    <Box>
      
    </Box>
  )
}

export default Card;